import styled from "styled-components";

const SignupContainer = styled.main `
    display: flex;
    justify-content: center;
    align-items: center;

    width: 100%;
    min-height: 100vh;
`
export {
    SignupContainer,
}